import { DragDirective } from './drag.directive';

describe('DragDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDirective(null);
    expect(directive).toBeTruthy();
  });
});
